"use client";
import { usePathname, useRouter, useSearchParams } from "next/navigation";
import { Search, SlidersHorizontal } from "lucide-react";
import { useEffect, useState } from "react";
export default function InvoiceFilters({clients}:{clients:{id:string;name:string;company:string|null}[]}){const router=useRouter();const pathname=usePathname();const sp=useSearchParams();const [q,setQ]=useState(sp.get("q")||""); useEffect(()=>{const t=setTimeout(()=>{const params=new URLSearchParams(sp.toString());q?params.set("q",q):params.delete("q");router.replace(`${pathname}?${params.toString()}`);},280);return()=>clearTimeout(t)},[q]); const set=(key:string,val:string)=>{const p=new URLSearchParams(sp.toString());val&&val!=="all"?p.set(key,val):p.delete(key);router.replace(`${pathname}?${p.toString()}`)};return <div className="rounded-2xl border border-slate-200 bg-white p-4 shadow-panel"><div className="grid gap-3 lg:grid-cols-[1fr_180px_220px_180px]"><div className="relative"><Search size={17} className="absolute left-3 top-3 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search invoice number or client..." className="w-full rounded-xl border border-line py-2.5 pl-10 pr-3 text-sm outline-none focus:border-brand-400 focus:ring-4 focus:ring-brand-50"/></div><select value={sp.get("status")||"all"} onChange={e=>set("status",e.target.value)} className="rounded-xl border border-line bg-white px-3 text-sm font-medium outline-none focus:border-brand-400"><option value="all">All statuses</option><option value="draft">Draft</option><option value="sent">Sent</option><option value="paid">Paid</option><option value="overdue">Overdue</option></select><select value={sp.get("clientId")||"all"} onChange={e=>set("clientId",e.target.value)} className="rounded-xl border border-line bg-white px-3 text-sm font-medium outline-none focus:border-brand-400"><option value="all">All clients</option>{clients.map(c=><option key={c.id} value={c.id}>{c.company?`${c.name} — ${c.company}`:c.name}</option>)}</select><select value={sp.get("sort")||"newest"} onChange={e=>set("sort",e.target.value)} className="rounded-xl border border-line bg-white px-3 text-sm font-medium outline-none focus:border-brand-400"><option value="newest">Newest first</option><option value="oldest">Oldest first</option><option value="due-soon">Due date</option></select></div><div className="mt-3 flex items-center gap-2 text-xs text-slate-400"><SlidersHorizontal size={13}/> Filters are applied on the server.</div></div>}
