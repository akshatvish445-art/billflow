"use client";
import { useMemo, useState } from "react";
import { Pencil, Plus, Search, Trash2, Users, X } from "lucide-react";
import { Button, EmptyState, Field } from "@/components/ui";

export default function ClientsTable({ initialClients }: { initialClients:any[] }){
 const [clients,setClients]=useState(initialClients); const [query,setQuery]=useState(""); const [editing,setEditing]=useState<any|null>(null); const [saving,setSaving]=useState(false); const [error,setError]=useState("");
 const filtered=useMemo(()=>clients.filter(c=>[c.name,c.email,c.company||""].join(" ").toLowerCase().includes(query.toLowerCase())),[clients,query]);
 const open=(client?:any)=>{setError("");setEditing(client||{name:"",email:"",company:"",address:"",phone:""});};
 const save=async(e:React.FormEvent)=>{e.preventDefault();setSaving(true);setError("");const method=editing.id?"PATCH":"POST";const url=editing.id?`/api/clients/${editing.id}`:"/api/clients";const res=await fetch(url,{method,headers:{"Content-Type":"application/json"},body:JSON.stringify(editing)});const data=await res.json();if(!res.ok){setError(data.error||"Could not save client");setSaving(false);return;}setClients(prev=>editing.id?prev.map(x=>x.id===data.client.id?{...data.client,_count:x._count}:x):[{...data.client,_count:{invoices:0}},...prev]);setEditing(null);setSaving(false);};
 const remove=async(id:string)=>{if(!confirm("Delete this client? Invoices belonging to this client will prevent deletion."))return;const res=await fetch(`/api/clients/${id}`,{method:"DELETE"});const data=await res.json();if(!res.ok){alert(data.error||"Could not delete client");return;}setClients(prev=>prev.filter(c=>c.id!==id));};
 return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-brand-600">Relationships</p><h1 className="mt-1 text-3xl font-black tracking-tight">Clients</h1><p className="mt-2 text-sm text-slate-500">Keep billing details tidy so every invoice starts clean.</p></div><Button onClick={()=>open()}><Plus size={17}/> Add client</Button></div><div className="rounded-2xl border border-slate-200 bg-white shadow-panel"><div className="border-b border-slate-100 p-4"><div className="relative max-w-md"><Search size={17} className="absolute left-3 top-3 text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search clients..." className="w-full rounded-xl border border-line py-2.5 pl-10 pr-3 text-sm outline-none focus:border-brand-400 focus:ring-4 focus:ring-brand-50"/></div></div>{filtered.length===0?<div className="p-5"><EmptyState title={clients.length?"No matching clients":"No clients yet"} description={clients.length?"Try a different search term.":"Add your first client before creating an invoice."} action={!clients.length&&<Button onClick={()=>open()}><Plus size={16}/> Add client</Button>}/></div>:<div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-400"><tr><th className="px-5 py-3.5">Client</th><th className="px-5 py-3.5">Contact</th><th className="px-5 py-3.5">Invoices</th><th className="px-5 py-3.5 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{filtered.map(c=><tr key={c.id} className="hover:bg-slate-50/80"><td className="px-5 py-4"><p className="font-bold">{c.name}</p><p className="text-xs text-slate-400">{c.company||"Independent"}</p></td><td className="px-5 py-4"><p>{c.email}</p><p className="text-xs text-slate-400">{c.phone||"—"}</p></td><td className="px-5 py-4 text-slate-600">{c._count.invoices}</td><td className="px-5 py-4"><div className="flex justify-end gap-2"><button onClick={()=>open(c)} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 hover:text-ink"><Pencil size={16}/></button><button onClick={()=>remove(c.id)} className="rounded-lg p-2 text-slate-500 hover:bg-rose-50 hover:text-rose-700"><Trash2 size={16}/></button></div></td></tr>)}</tbody></table></div>}</div>{editing&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4" role="dialog" aria-modal="true"><div className="w-full max-w-xl rounded-3xl bg-white p-6 shadow-soft sm:p-7"><div className="flex items-center justify-between"><div><h2 className="text-xl font-black">{editing.id?"Edit client":"Add client"}</h2><p className="mt-1 text-sm text-slate-500">Billing contact details for future invoices.</p></div><button onClick={()=>setEditing(null)} className="rounded-lg p-2 text-slate-400 hover:bg-slate-100"><X size={18}/></button></div><form onSubmit={save} className="mt-6 grid gap-4 sm:grid-cols-2"><Field label="Full name" required value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})}/><Field label="Email" type="email" required value={editing.email} onChange={e=>setEditing({...editing,email:e.target.value})}/><Field label="Company" value={editing.company||""} onChange={e=>setEditing({...editing,company:e.target.value})}/><Field label="Phone" value={editing.phone||""} onChange={e=>setEditing({...editing,phone:e.target.value})}/><label className="sm:col-span-2"><span className="text-sm font-semibold text-slate-800">Address</span><textarea rows={3} value={editing.address||""} onChange={e=>setEditing({...editing,address:e.target.value})} className="mt-1.5 w-full rounded-xl border border-line px-3.5 py-3 text-sm outline-none focus:border-brand-400 focus:ring-4 focus:ring-brand-50"/></label>{error&&<p className="sm:col-span-2 rounded-xl bg-rose-50 px-3.5 py-3 text-sm font-medium text-rose-700">{error}</p>}<div className="flex justify-end gap-2 sm:col-span-2"><Button type="button" variant="secondary" onClick={()=>setEditing(null)}>Cancel</Button><Button type="submit" loading={saving}>{editing.id?"Save changes":"Add client"}</Button></div></form></div></div>}</div>
}
