"use client";
import { useState } from "react";
import { Check, ImagePlus, Save } from "lucide-react";
import { Button, Field } from "@/components/ui";
export default function SettingsForm({user,currencies}:{user:any;currencies:{code:string;label:string;symbol:string}[]}){const [form,setForm]=useState(user);const [saving,setSaving]=useState(false);const [saved,setSaved]=useState(false);const [error,setError]=useState("");const file=(e:React.ChangeEvent<HTMLInputElement>)=>{const f=e.target.files?.[0];if(!f)return;if(f.size>4_500_000){setError("Logo is too large. Keep it under 4.5 MB.");return;}const reader=new FileReader();reader.onload=()=>setForm((x:any)=>({...x,logoData:String(reader.result)}));reader.readAsDataURL(f)};const save=async(e:React.FormEvent)=>{e.preventDefault();setSaving(true);setSaved(false);setError("");const res=await fetch("/api/settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const data=await res.json();if(!res.ok){setError(data.error||"Could not save settings");setSaving(false);return;}setForm(data.user);setSaving(false);setSaved(true);setTimeout(()=>setSaved(false),2200)};return <div className="mx-auto max-w-3xl space-y-6"><div><p className="text-sm font-semibold text-brand-600">Workspace identity</p><h1 className="mt-1 text-3xl font-black tracking-tight">Settings</h1><p className="mt-2 text-sm text-slate-500">Customize how your business appears across every invoice.</p></div><form onSubmit={save} className="space-y-5"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-panel sm:p-6"><h2 className="font-black">Business profile</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Business name" required value={form.businessName} onChange={e=>setForm({...form,businessName:e.target.value})}/><Field label="Invoice number prefix" required value={form.invoicePrefix} onChange={e=>setForm({...form,invoicePrefix:e.target.value.toUpperCase()})} hint="Example: NST → NST-0001"/><label className="block"><span className="text-sm font-semibold text-slate-800">Currency</span><select value={form.currency} onChange={e=>setForm({...form,currency:e.target.value})} className="mt-1.5 w-full rounded-xl border border-line bg-white px-3.5 py-3 text-sm outline-none focus:border-brand-400 focus:ring-4 focus:ring-brand-50">{currencies.map(c=><option key={c.code} value={c.code}>{c.code} — {c.label} ({c.symbol})</option>)}</select></label></div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-panel sm:p-6"><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center overflow-hidden rounded-2xl bg-ink text-sm font-black text-white">{form.logoData?<img src={form.logoData} alt="Business logo" className="h-full w-full object-cover"/>:<ImagePlus size={22}/>}</div><div><h2 className="font-black">Business logo</h2><p className="mt-1 text-xs text-slate-400">PNG, JPG or SVG data under 4.5 MB. Stored with your workspace.</p><label className="mt-3 inline-flex cursor-pointer items-center gap-2 rounded-xl border border-line px-3 py-2 text-xs font-bold hover:bg-slate-50"><ImagePlus size={14}/> Choose file<input type="file" accept="image/png,image/jpeg,image/svg+xml" className="hidden" onChange={file}/></label>{form.logoData&&<button type="button" onClick={()=>setForm({...form,logoData:null})} className="ml-2 text-xs font-bold text-rose-600">Remove</button>}</div></div></section>{error&&<div className="rounded-2xl bg-rose-50 p-4 text-sm font-semibold text-rose-700">{error}</div>}<div className="flex justify-end"><Button type="submit" loading={saving}>{saved?<><Check size={16}/> Saved</>:<><Save size={16}/> Save settings</>}</Button></div></form></div>}
