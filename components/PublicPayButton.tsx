"use client";
import { CheckCircle2, CreditCard, Loader2 } from "lucide-react";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function PublicPayButton({token,status}:{token:string;status:string}){const router=useRouter(); const [loading,setLoading]=useState(false);const [done,setDone]=useState(false);const [error,setError]=useState("");const pay=async()=>{setLoading(true);setError("");const res=await fetch(`/api/public/invoices/${token}/pay`,{method:"POST"});const data=await res.json();if(!res.ok){setError(data.error||"Payment failed");setLoading(false);return;}setDone(true);setLoading(false);router.refresh()};if(done)return <div className="flex items-center gap-3 rounded-2xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800"><CheckCircle2 size={20}/> Payment received. Thank you.</div>;return <div className="rounded-2xl bg-ink p-5 text-white"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="font-black">Ready to pay?</p><p className="mt-1 text-sm text-slate-300">Demo mode securely marks this invoice as paid.</p></div><button onClick={pay} disabled={loading||status==="draft"} className="inline-flex items-center justify-center gap-2 rounded-xl bg-white px-5 py-3 text-sm font-black text-ink disabled:opacity-60">{loading?<Loader2 className="animate-spin" size={16}/>:<CreditCard size={16}/>} {loading?"Processing...":"Pay invoice"}</button></div>{error&&<p className="mt-3 text-sm font-medium text-rose-300">{error}</p>}</div>}
