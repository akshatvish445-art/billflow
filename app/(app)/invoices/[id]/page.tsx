import { notFound } from "next/navigation";
import Link from "next/link";
import { requireUser } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { effectiveStatus, invoiceTotals, money, formatDate } from "@/lib/utils";
import InvoiceActions from "@/components/InvoiceActions";
import { StatusPill } from "@/components/ui";

export default async function InvoicePage({ params }: { params: Promise<{id:string}> }){const user=await requireUser();const {id}=await params;const invoice=await prisma.invoice.findFirst({where:{id,userId:user.id},include:{client:true,lineItems:{orderBy:{createdAt:"asc"}}}});if(!invoice)notFound();const status=effectiveStatus(invoice);const totals=invoiceTotals(invoice);const publicUrl=`${process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000"}/invoice/${invoice.publicToken}`;return <div className="space-y-5"><div className="no-print flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><Link href="/invoices" className="text-sm font-bold text-slate-500 hover:text-ink">← Back to invoices</Link><div className="mt-2 flex items-center gap-3"><h1 className="text-2xl font-black">{invoice.number}</h1><StatusPill status={status}/></div></div><InvoiceActions invoiceId={invoice.id} publicUrl={publicUrl} status={status}/></div><article className="print-page mx-auto max-w-4xl rounded-3xl border border-slate-200 bg-white p-6 shadow-soft sm:p-10"><header className="flex flex-col justify-between gap-7 border-b border-slate-200 pb-8 sm:flex-row"><div><div className="flex items-center gap-3">{user.logoData?<img src={user.logoData} alt="Business logo" className="h-12 w-12 rounded-xl object-cover"/>:<div className="flex h-12 w-12 items-center justify-center rounded-xl bg-ink text-sm font-black text-white">{user.businessName.slice(0,2).toUpperCase()}</div>}<div><p className="text-xl font-black">{user.businessName}</p><p className="text-xs text-slate-400">Professional invoicing with BillFlow</p></div></div></div><div className="text-left sm:text-right"><p className="text-xs font-bold uppercase tracking-[.18em] text-slate-400">Invoice</p><p className="mt-1 text-2xl font-black">{invoice.number}</p><div className="mt-2"><StatusPill status={status}/></div></div></header><div className="grid gap-7 border-b border-slate-200 py-8 sm:grid-cols-3"><div><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Bill to</p><p className="mt-2 font-black">{invoice.client.name}</p><p className="mt-1 text-sm text-slate-500">{invoice.client.company}</p><p className="mt-1 whitespace-pre-line text-sm leading-6 text-slate-500">{invoice.client.address}</p><p className="mt-1 text-sm text-slate-500">{invoice.client.email}</p></div><div><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Issue date</p><p className="mt-2 font-bold">{formatDate(invoice.issueDate)}</p></div><div><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Due date</p><p className={`mt-2 font-bold ${status==="overdue"?"text-rose-600":""}`}>{formatDate(invoice.dueDate)}</p></div></div><div className="overflow-x-auto py-7"><table className="w-full min-w-[550px] text-left text-sm"><thead className="border-b border-slate-200 text-xs uppercase tracking-wider text-slate-400"><tr><th className="py-3">Description</th><th className="py-3 text-right">Qty</th><th className="py-3 text-right">Rate</th><th className="py-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-slate-100">{invoice.lineItems.map(item=><tr key={item.id}><td className="py-4 pr-4 font-medium">{item.description}</td><td className="py-4 text-right text-slate-500">{Number(item.quantity)}</td><td className="py-4 text-right text-slate-500">{money(Number(item.rate),user.currency)}</td><td className="py-4 text-right font-bold">{money(Number(item.quantity)*Number(item.rate),user.currency)}</td></tr>)}</tbody></table></div><div className="flex justify-end"><div className="w-full max-w-sm space-y-3 text-sm"><div className="flex justify-between"><span className="text-slate-500">Subtotal</span><span>{money(totals.subtotal,user.currency)}</span></div><div className="flex justify-between"><span className="text-slate-500">Discount ({Number(invoice.discountRate)}%)</span><span>-{money(totals.discount,user.currency)}</span></div><div className="flex justify-between"><span className="text-slate-500">Tax ({Number(invoice.taxRate)}%)</span><span>{money(totals.tax,user.currency)}</span></div><div className="mt-3 flex justify-between border-t border-slate-200 pt-4 text-lg"><span className="font-black">Total due</span><span className="font-black">{money(totals.total,user.currency)}</span></div></div></div>{invoice.notes&&<div className="mt-8 rounded-2xl bg-slate-50 p-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Notes</p><p className="mt-2 whitespace-pre-line text-sm leading-6 text-slate-600">{invoice.notes}</p></div>}<footer className="mt-10 border-t border-slate-100 pt-6 text-xs text-slate-400">Thanks for your business. Please use the secure payment link from the invoice to pay.</footer></article></div>
}
